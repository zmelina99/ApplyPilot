# Personal data & secrets — NEVER commit (repo is public)
.env
.env.*.local

# Resume files — local only; README stays tracked
resumes/*
!resumes/README.md

# Node / TypeScript build artifacts
node_modules/
dist/
web/dist/
.vite/
*.tsbuildinfo

# Local Claude Code settings (per-machine, not shared)
.claude/settings.local.json

# OS cruft
.DS_Store
